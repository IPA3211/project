소코반 프로젝트

이 파일은 프로젝트 조원들에게 안내하기위해 작성됨

project.c에 대해..

1. project 진행의 매인 소스코드
2. common var 은 여러 함수에서 공용으로 사용해야하는 '변수'가 들어간다.
3. common func는 본인이 재작하지 않았고 우리힘으로 하기 힘든 코드를 긁어 왔을때 추가한다.
4. message 는 조원들이 알아야할 사항들을 기입해 둔다.
5. todo는 해야 할 일들을 정리해 둔다. (기능이 구현되면 clear 를 적어둔다 뒤에 괄호는 main에서 기능이 실행되는지 나타내므로 조장이 추가한다.)
6. 본인 이름이 적힌 주석 아래에 본인이 작성한 함수를 작성한후 조장에게 말한다.(조장이 main함수에 순서에 맞게 기능을 추가할 것임)
7. main 함수를 작성할 때에는 프로그램의 진행순서를 고려해야 한다.(되도록 수정하지 말것)

규칙
1. master 브렌치는 되도록이면 건들지 않는다.
2. project.c 파일 하나로만 진행해야 하는 프로젝트이므로 master 브렌치에는 readme, project.c, map, ranking, sokoban 파일만 있게 한다.(정보를 공유하는 파일은 있어도 됨, 여러파일 만들지 말자는 의미)
3. 자기가 구현하는 기능은 테스트를 충분히 진행한후 조장에게 말하고 master 와 본인이 작성한 branch 를 백업하고 merge한다.
4. 자신이 만든 함수는 자신의 이름이 적힌 주석아래에 추가한다. 
5. 남의 branch 에 들어가서 파일을 수정하고 commit 및 push 하지 않는다.(참고는 할 수 있음)

# 수정 사항이 생길 수 있음
